kotlin.code.style=official

gradleVersion=9.4.0

# The Kotlin compiler needs a large heap to build the course modules.
# Without this, the build can fail with OutOfMemoryError during compileKotlin.
org.gradle.jvmargs=-Xmx6g
kotlin.compiler.execution.strategy=in-process